namespace dp_record
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            /*
             * - parse command line to get interval
             * - print initial msg
             * - start timer with interval
             * - for each tick:
             *     - check if there are any changes
             *     - if there are changes then commit them
             */
        }
    }
}